{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://marktverbund25.at/get_limit_status_res.schema.json",
    "title": "limit order status response",
    "description": "format of response body lim. order status",
    "type": "object",
    "properties": {
        "limId": {
            "type": "string"
        },
        "status": {
            "type": "string",
            "enum": [
                "pending",
                "fulfilled",
                "expired",
                "canceled"
            ]
        }
    },
    "required": [
        "limId",
        "status"
    ]
}