{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://marktverbund25.at/cancel_limit_req.schema.json",
  "title": "cancel limit order request",
  "description": "format of request body to cancel a limit order on a product",
  "type": "object",
  "properties": {
        "lim_id": { "type": "number", "exclusiveMinimum": 0}
    },
    "required": ["lim_id"]
}
